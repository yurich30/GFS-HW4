import styled from 'styled-components';

export const DescriptionComponetStyled = styled.p`
  color: ${(props) => props.theme.text};
  margin-bottom: 56px;
`;

export const TextLink = styled.a`
  color: ${(props) => props.theme.textLink};
`;
